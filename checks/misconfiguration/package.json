{
  "name": "@securityscan/check-misconfiguration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@securityscan/contracts": "workspace:*",
    "@securityscan/detection-engine": "workspace:*",
    "@securityscan/http-client": "workspace:*",
    "@securityscan/security-rules": "workspace:*",
    "@securityscan/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
